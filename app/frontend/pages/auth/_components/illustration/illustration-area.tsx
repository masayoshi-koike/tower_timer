interface Props {
  elapsedTime: number;
  status: string;
  stage: number;
  isPlaying: boolean;
  isFinished: boolean;
}

export default function IllustrationArea({
  elapsedTime,
  status,
  stage,
  isPlaying,
  isFinished
}: Props) {

  return (
    <>
    </>
  );
}
